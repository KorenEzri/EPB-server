export const What = () => {
  return `GraphQL is a query language for APIs and a runtime for fulfilling those queries with your existing data. GraphQL provides a complete and understandable description of the data in your API, gives clients the power to ask for exactly what they need and nothing more, makes it easier to evolve APIs over time, and enables powerful developer tools.`;
};
export const Why = () => {
  return `Ask for what you need, get exactly that Get many resources in a single request Describe what’s possible with a type system`;
};

export const How = () => {
  return `In a simple graphQL service, the incoming query is parsed, and the query fields are executed and resolved against the schema to produce the result.`;
};
